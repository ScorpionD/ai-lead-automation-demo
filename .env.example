# This release always uses an in-browser mock. No variables are required.
# Reserved for a future backend adapter (not read by this MVP):
# VITE_API_BASE_URL=https://your-backend.example.com
# VITE_* variables are public. Never put secrets here.
